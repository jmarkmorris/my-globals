* This repo contains global files, to which I can symbolic link from any project.
* Then my default aider imports from README-*topic* will work anywhere.
* In new projects need to make the symlinks, so it is not automatic.
* I can put the symlinks in .gitignore as well.